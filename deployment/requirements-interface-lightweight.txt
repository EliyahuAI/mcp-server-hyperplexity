# Lightweight Interface Lambda Requirements
# Minimal dependencies for API routing, balance checks, file uploads ONLY
# NO AI operations, NO heavy processing, NO report generation

# Core AWS (aioboto3 needed for ai_api_client imports - table_maker → interview → ai_api_client)
# NOTE: boto3 and botocore are provided by AWS Lambda runtime - DO NOT package them
# Packaging boto3/botocore causes version conflicts with Lambda's built-in versions
aioboto3>=15.4.0  # Latest (Oct 18, 2025) - uses Lambda's built-in boto3/botocore

# File upload handling
python-multipart>=0.0.5

# Excel handling (needed by validator_invoker which is imported by generate_config_unified)
openpyxl>=3.0.0

# Config files
pyyaml>=6.0.0

# HTTP requests (lightweight)
requests>=2.25.0

# Async HTTP (needed by ai_api_client import chain, even though not executed in lightweight mode)
aiohttp>=3.8.0
async-timeout>=4.0.0  # Required by aiohttp

# Type hints
typing-extensions>=4.0.0

# JSON schema validation (lightweight)
jsonschema>=4.0.0

# Note: Removed heavy dependencies that are in background Lambda:
# - PyMuPDF (24MB) - PDF generation (background only)
# - xlsxwriter (175KB) - Excel writing (background only)
# - aioboto3 (async AWS - background only)
