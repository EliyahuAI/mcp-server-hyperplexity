# Use latest aioboto3 (15.4.0 released Oct 18, 2025) for modern boto3/botocore compatibility
# NOTE: boto3 and botocore are provided by AWS Lambda runtime - DO NOT package them
# Packaging boto3/botocore causes version conflicts with Lambda's built-in versions
aioboto3>=15.4.0

# Google Auth for Vertex AI API calls (lightweight, ~2MB vs 160MB for google-cloud-aiplatform)
# We use direct HTTP API calls, not the full SDK
google-auth>=2.0.0
requests>=2.25.0  # Required by google-auth for credential refresh

aiohttp>=3.8.0
python-dateutil>=2.8.2
typing_extensions>=4.0.0
async_timeout>=4.0.0
attrs>=21.2.0
multidict>=6.0.0
yarl>=1.8.0
frozenlist>=1.3.0
idna>=3.3
pyyaml>=6.0
jsonpatch>=1.33  # For AI-powered JSON Patch refinements (RFC 6902)
openpyxl>=3.0.0
reportlab>=3.6.0
beautifulsoup4>=4.9.0  # For HTML table parsing in search_memory
lxml>=4.9.0  # HTML parser for BeautifulSoup 