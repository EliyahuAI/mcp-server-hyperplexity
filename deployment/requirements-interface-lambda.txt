# Use latest aioboto3 (15.4.0 released Oct 18, 2025) for modern boto3/botocore compatibility
# NOTE: boto3 and botocore are provided by AWS Lambda runtime - DO NOT package them
# Packaging boto3/botocore causes version conflicts with Lambda's built-in versions
aioboto3>=15.4.0  # Latest version supports modern botocore

# Google Auth for Vertex AI API calls (lightweight, ~2MB vs 160MB for google-cloud-aiplatform)
# We use direct HTTP API calls via aiohttp, not the full SDK
google-auth>=2.0.0

python-multipart>=0.0.5
openpyxl>=3.0.0
typing-extensions>=4.0.0
PyJWT>=2.8.0  # For session token authentication
xlsxwriter>=3.0.0
aiohttp>=3.8.0
async-timeout>=4.0.0
pyyaml>=6.0.0
requests>=2.25.0
PyMuPDF>=1.24.0
pymupdf4llm>=0.0.5
jsonschema>=4.0.0
beautifulsoup4>=4.9.0  # For HTML table parsing in table_extractor
lxml>=4.9.0  # HTML parser for BeautifulSoup 