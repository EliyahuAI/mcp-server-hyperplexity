name: Subindex
description: >-
  Generate, validate, and fact-check research tables with AI-sourced citations
  and per-cell confidence scores. Upload an Excel/CSV or describe a table in
  plain language — Subindex researches every cell, scores confidence, and
  links citations. Supports table generation, validation, reference checking,
  and result export.
homepage: https://subindex.ai

startCommand:
  type: http
  configSchema:
    type: object
    required:
      - apiKey
    properties:
      apiKey:
        type: string
        title: Subindex API Key
        description: >-
          Your Subindex API key. Get one (with $20 free credits) at
          https://subindex.ai/account
        x-from:
          header: X-Api-Key
        x-to:
          header: X-Api-Key
