{
  "type": "object",
  "required": ["trigger_execution", "show_structure", "ai_message", "context_web_research", "processing_steps", "table_name"],
  "properties": {
    "trigger_execution": {
      "type": "boolean",
      "description": "Set to true ONLY when user has explicitly approved (said 'yes', 'go for it', 'looks good'). Set to false when asking questions OR when showing structure for approval."
    },
    "show_structure": {
      "type": "boolean",
      "description": "Set to true when proposing a table structure for approval. Set to false when asking questions OR when user has approved and execution is starting."
    },
    "ai_message": {
      "type": "string",
      "minLength": 10,
      "description": "Your response to the user. REQUIRED. Three modes: (1) Questions only, (2) Table structure + 'Ready to generate?', (3) Confirmation + specific value statement. Use markdown formatting."
    },
    "context_web_research": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Specific research items the LLM doesn't know: specific entities, very recent info, specialized knowledge. Empty when asking questions."
    },
    "processing_steps": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 0,
      "maxItems": 10,
      "description": "Specific action phrases with context (e.g., 'Researching Eliyahu.AI Background', 'Finding GenAI Job Postings'). NOT generic phrases like 'Finding Companies'. Required when trigger_execution=true or show_structure=true."
    },
    "table_name": {
      "type": "string",
      "description": "Table name in Title Case. Required when trigger_execution=true or show_structure=true. Empty when asking questions."
    }
  }
}
