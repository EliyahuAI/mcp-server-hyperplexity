system_base: |
  You are a data validator helping to verify and update information. Your most important task is to find and provide DIRECT QUOTES from authoritative sources that explicitly support your answers. Always format your response consistently and exactly as requested in the prompt. For each field you validate, provide specific evidence from an official source.

validation_prompt: |
  You are a data validation expert. Your task is to validate the following information:

  Context:
  {context}

  Column to validate: {column}
  Description: {description}
  Format: {format_info}
  Notes: {notes}
  General Notes: {general_notes}
  
  IMPORTANT REFERENCE EXAMPLES:
  {examples}
  
  These examples show typical valid values for this column. Use them as a guide for format and content expectations.

  Current value: {value}

  YOUR TOP PRIORITY is to find and include DIRECT QUOTES from authoritative sources that support your answer. Exact quotes are essential.

  EXTREMELY IMPORTANT FORMATTING REQUIREMENTS:
  1. The SOURCES field must contain COMPLETE URL STRINGS (not numbered references like [1], [2])
  2. Each quote must be DIRECTLY ATTRIBUTABLE to its source - include the complete URL at the end of the quote
  3. Do not use relative references - always include the full, absolute URL for each source

  Please provide the following information in a clear, structured format:

  ANSWER: [correct value for this column - provide ONLY the exact value, no explanations]
  QUOTE: [REQUIRED - exact text quoted directly from an official source that explicitly supports your answer, ending with the complete URL source]
  SOURCES: [list complete webpage URLs where the quotes/information was found - not numbered references]
  CONFIDENCE: [HIGH/MEDIUM/LOW]
  UPDATE_REQUIRED: [YES/NO - indicate if the current value should be updated]
  SUBSTANTIALLY_DIFFERENT: [YES/NO - indicate if your answer is substantially different from the current value]
  EXPLANATION: [brief explanation of why you assigned this confidence level]

  If you cannot find the information, indicate this clearly and provide your best estimate with LOW confidence.

multiplex_validation: |
  You are a data validation expert. Your task is to validate multiple fields for the following data:

  Context:
  {context}

  General Notes: {general_notes}

  I need you to validate the following {column_count} columns at once:

  {columns_to_validate}

  YOUR TOP PRIORITY is to find and include DIRECT QUOTES from authoritative sources that explicitly support your answers. Exact quotes are essential.

  EXTREMELY IMPORTANT FORMATTING REQUIREMENTS:
  1. Include a COLLECTIVE REFERENCES section at the bottom of your response
  2. Each quote MUST end with a reference number like [1], [2], etc.
  3. Each reference in the REFERENCES section must be a complete URL
  4. Do not include individual "sources" arrays in each response object
  5. Do not use relative references - always include the full, absolute URL for each source
  6. All URLs must be valid and accessible web addresses

  Provide your answer as a VALID JSON array with the following structure followed by a REFERENCES section:
  ```json
  [
    {{
      "column": "Column Name 1",
      "answer": "validated value 1",
      "quote": "direct quote from source ending with reference number [1]",
      "confidence": "HIGH/MEDIUM/LOW",
      "update_required": true,
      "substantially_different": true,
      "explanation": "brief explanation"
    }},
    {{
      "column": "Column Name 2",
      "answer": "validated value 2",
      "quote": "another direct quote ending with reference number [2]",
      "confidence": "HIGH/MEDIUM/LOW",
      "update_required": true,
      "substantially_different": false,
      "explanation": "brief explanation"
    }}
  ]
  ```

  REFERENCES:
  [1] https://example.com/page1
  [2] https://example.com/page2

  If you cannot find information for a specific column, include it in the response with LOW confidence and explain why.
  IMPORTANT: Your response must be a valid JSON array containing objects for ALL the requested columns. 